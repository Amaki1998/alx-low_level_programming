#include "main.h"
/**
 * is_palindrome - function that find a palindrome string
 * @s: string
 * Return: returns 1 if is palindrome, otherwise 0
 */
int is_palindrome(char *s)
{
	int x;

	x = 1;

	if (*s == x)
		return (1);
	return (0);
}
