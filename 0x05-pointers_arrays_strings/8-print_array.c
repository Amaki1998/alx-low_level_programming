#include <stdio.h>
#include "main.h"
/**
 * print_array - function
 *
 * @a: pointer of parameter
 * @n: parameter
 */
void print_array(int *a, int n)
{
	int x = 0;

	if (y > 0)
	{
		while (x < y - 1)
			printf("%d, ", a[i++]);
		printf("%d\n", a[i]);
	}
	printf("\n");
}
